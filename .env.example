# dnd5e-engine 环境配置模板
# 用法：复制为 .env 并填入真实值。.env 已被 gitignore，绝不要提交真实 key。
#   cp .env.example .env   然后编辑 .env

# --- M1-3 语义检索（OpenAI embeddings） ---
OPENAI_EMBEDDING_API_KEY=
# 可选覆盖（不填则用默认）：
# OPENAI_EMBEDDING_MODEL=text-embedding-3-large
# OPENAI_EMBEDDING_BASE_URL=https://api.openai.com/v1

# --- Phase 2 语音（OpenAI Realtime） ---
OPENAI_VOICE_API_KEY=
# OPENAI_VOICE_MODEL=gpt-realtime
# OPENAI_VOICE_BASE_URL=https://api.openai.com/v1

# --- 资产 vault 位置（可选，默认仓库内 vault/） ---
# DND5E_VAULT=
